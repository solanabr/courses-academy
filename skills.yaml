# Skill-tag vocabulary (gate-19). Every entry here must be applied to ≥2 live
# lessons (reviewExempt entries excepted), and every lesson skill must resolve
# here. Pruned 2026-07-31 to the slugs the live course (btc-to-sol-evolution)
# actually uses; the entries for the parked track-1 catalog were removed with it
# and come back together with the courses in courses/_draft/ — restore them from
# this file's history in the same PR that un-drafts the courses.
- slug: solana-fundamentals
  label: Solana Fundamentals
- slug: account-model
  label: Account Model
- slug: transactions
  label: Transactions
- slug: keypairs-wallets
  label: Keypairs & Wallets
- slug: rpc-reads
  label: RPC Reads
- slug: solana-programs
  label: Program Development
- slug: anchor
  label: Anchor Framework
- slug: solana-kit
  label: Solana Kit

# Added for courses/digital-assets (2026-09-07), under §5 policy 3 of
# courses/CATALOG.md. Only slugs with NO equivalent in the sanctioned migration
# list are added here: the token/program/client vocabulary that course also uses
# (token-2022, spl-tokens, pdas, signers, compute-budget, checked-arithmetic,
# error-handling, version-pinning, program-security) is already being added by
# the solana-payments-commerce branch, and re-adding it here would produce a
# duplicate on merge. Each of the four below lands on >= 2 digital-assets
# lessons, per policy 4.
- slug: token-metadata
  label: Token & Asset Metadata
- slug: state-compression
  label: State Compression
- slug: merkle-proofs
  label: Merkle Proofs
- slug: das-api
  label: Digital Asset Standard API
