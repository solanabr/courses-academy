[package]
name = "counter_v1_twin"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]

[features]
default = []
no-entrypoint = []
cpi = ["no-entrypoint"]
idl-build = ["anchor-lang/idl-build"]

[dependencies]
# The twin the learner "already knows": the stable Anchor 1.x line.
anchor-lang = "1.1.2"

[dev-dependencies]
solana-sdk = "3"

bincode = "1"

[workspace]
